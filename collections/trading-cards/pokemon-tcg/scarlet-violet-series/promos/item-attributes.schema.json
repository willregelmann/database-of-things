{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Scarlet & Violet era promos number override — bare digits, no total",
  "type": "object",
  "properties": {
    "number": {
      "type": "string",
      "pattern": "^[0-9]+$"
    }
  }
}
